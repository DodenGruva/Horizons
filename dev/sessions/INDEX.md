# Vintage Horizons — session record index

> Tier 3 history. Newest first. Read this index; open individual sessions only when a row is relevant.

| # | Date | Version | Compatibility change | Summary |
|---|---|---|---|---|
| 50 | 2026-08-24 | 0.3.101, human-accepted | - | Diagnosed the Phase 8 precise-angle flicker from source without running 0.3.100's capture, and fixed it. The depth pyramid halves with floor and folds the odd leftover row into its last texel, so a level-L texel covers exactly 2^L pixels; both copies of the box test instead scaled by the level's texel COUNT, which agrees only while `levelSize * 2^level == screenSize`. At 1440 rows the chain reaches 45 and then 22, so from level 6 up the sampled rectangle fell one texel short at the top edge and skipped precisely the texel holding sky beyond an occluder's silhouette - declaring a box peeking over a ridge hidden, which invariant 8 forbids. Under the split, one such wrongly culled NEAR command leaves an exact `1.0` hole in the mid-frame picture and flips whole FAR clusters between `occluded` and `background`, which is the visible artifact and which also explains why the margin ladder (a gap no bias closes) and the 0.3.99 perimeter guard (the hole is interior, not perimeter) both failed. The fix anchors the rectangle in screen pixels and shifts down by level, clamping the far edge to the last texel to honour the fold, applied identically to the GLSL and its C# mirror. Gated on reproduction: the unfixed tier failed exactly one of 5,089 assertions, the predicted peeking box, with all five controls passing. Post-fix 5,089 pass and the owner confirms the flicker is gone. G96/G97 record the texel-footprint rule and that a mirrored reference cannot catch a shared assumption. |
| 49 | 2026-08-24 | 0.3.96 through 0.3.100; 0.3.100 installed, not human-run | - | Proved Phase 8's value in the exact live draw stream: at the owner's ridge view split-far removed 80.5% of commands/77.1% of indices and FPS rose roughly 340 to 460. Rejected the 4/16/64/256 bias ladder and then the 0.3.99 sky-silhouette guard after flicker remained bad and the owner clarified that most affected meshes sit inside terrain. Found that raw verdict ranking put 6,592 draw-safe changes ahead of 8,666 real culling changes. Version 0.3.100 captures split-near and split-far, separates command presence from cull-state transitions, and labels true offenders by screen region without crosshair targeting. 5,073 assertions pass; the next action is one still-camera `.vhflicker` capture and both bucket reports. Older Mods-folder zips are now explicitly preserved. |
| 48 | 2026-08-24 | 0.3.92 through 0.3.95, human-accepted | - | Replaced a 75.1-second zero-work cache join and camera-triggered refinement with bounded exact-row demand before the empty-mesh return. Eight orientation-independent lanes now form a coarse-to-fine outward wave; an independent under-player foundation reaches L0, late tint slots block only affected meshes until proper colors are ready, and the closest band receives 24/6 of the unchanged 32/8 allowance while outward retains one outstanding slot per direction. The owner accepted startup, sharpening, colors, and final pacing. 5,013 assertions pass; 0.3.95 is installed. Phase 8 resumes at the controlled adjacent `late` versus `cull` comparison. |
| 47 | 2026-08-24 | 0.3.87 clusters through 0.3.91 preset ladder | - | Built the measured 4x4 cluster branch: one scene improved about 360 to 390 FPS, but clustered HZB decisions multiplied a pre-existing precise-angle flicker. `.vhcull off` stopped every old/new case and geometry remained normally shaped, isolating the depth verdict; added a four-step 24-bit fail-open band. Repaired invalid testing twice: seven switches became one cumulative `.vhphase8 off/batch/cull/late/packed/clusters` ladder, then repeated active presets stopped re-requesting arenas and re-meshing 1,678 sections. The valid full-stack run flickered and lost to legacy; `late` retained only the one older flicker, while its 362 FPS was warm-up-contaminated. Phase 8 is paused. The next priority is cache startup/refinement: a 5,317-section join had zero dirty/load/mesh work at 10 and 30 seconds, first mesh at 75.1 seconds, and source tracing confirms both an empty-mesh bootstrap cycle and frustum-gated demand that starves terrain behind the camera. A dedicated coarse-360-then-radial-refinement plan is ready. 0.3.91 is installed; 4,980 assertions pass. |
| 46 | 2026-08-24 | 0.3.84 split accepted; 0.3.85 packed arrays rejected; 0.3.86 indexed packed accepted | - | The owner accepted the same-frame near/far depth split. Built Phase 7's exact 12-byte opaque quads and 0.3.85 matched the picture, but the same scene fell from about 300 to 260 FPS: six draw-arrays decoder invocations lost to the expanded path's four indexed corners. Rejected that topology and built 0.3.86 with one reusable index pattern, four virtual corners per quad, the same arena/record/fallback, and no per-section index storage. The owner reports exact visual and FPS parity with packing off and on, accepting the indexed format on the primary driver. Packing is neutral there; paired-route, second-driver, and final memory-policy gates remain. Cluster subdivision is now the explicit Phase 8 implementation branch. 3,911 assertions pass. |
| 45 | 2026-08-24 | 0.3.83 packaged, installed and human-played | - | Started Phase 6 and immediately corrected the number it was justified by: the TODO's "38.2% to 61.1%" compared two different populations, and the honest per-band gain is 4-6 points. Built two harness modes to choose between the plan's options offline - a full-scene occluder and a camera-motion model with a stale-versus-fresh safety comparison whose floor is zero by construction - and chose the previous-frame picture over the near/far split on measurement, after withdrawing the recommendation once for having measured it with a still camera. The owner corrected the world model too: his cache is far-band starved, and re-weighting onto a grown world tripled the prize and killed the structural argument against the split. Built `.vhlate`, the camera-delta matrix re-base, a pure five-reason refusal policy and a geometry-revision guard. Then four instruments failed in front of the owner, each safely and each invisibly: the picture never built at all (0 of 4,047, shader conflict), the classify pass discarded 99.9% of the work it paid for at 149us a frame, the cull shader was created behind a check that never ran, and the GPU timer counted minimised frames and reported 4.8us for a 24us pass. Separately the arena's fixed 256 MiB was refusing 322 sections and capping the batched path at 35% coverage - two sessions of comparisons had been measuring the pool rather than the feature; sizing it from the draw distance took coverage to 100% and the owner from 300 to 480 FPS. The deciding measurement came from an ordinary hilltop: 0% hidden with the old arrangement against 8/20/35% with the new one, at the same cost. Then the owner played it and saw mid-screen flicker while turning, which disposes of the edge theory a guard had just been built on and of the case for a one-frame-old picture - Phase 6 will use the split instead, in a fresh session. 3,696 assertions pass. |
| 44 | 2026-08-23 | 0.3.71 packaged; 0.3.70 human-played | - | Asked why the testing kept costing whole evenings, and the answer was that Phase 4 produces no visible change, so its only output is a number - and three playtests had measured broken instruments instead of terrain. Moved the question offline: the cache decoder, mesher, pyramid reduction and box test all already run on the CPU, so `HzbField` reconstructs the same measurement from the owner's real cache in 50 seconds. Its first run was wrong twice (no frustum cull, cameras placed on hilltops) and both faults produced believable tables - offline that cost ten minutes rather than two evenings. Corrected, it chose wider sampling over cluster subdivision by about two to one, and then reproduced the game's own 0-1k figure at the same view distance, which is the first time offline and in-game numbers were compared on equal terms. Shipped the widening, then built Phase 5 whole: per-command cull boxes in command order, the command buffer as a compute target, a cull shader sharing one copy of the box test, same-frame ordering that removes stale suppression entirely, and the old queries stood down. Two playtests: the first looked correct but could not be attributed, because the periodic report fires once at 30s and `.vhcull` only printed to chat; the second confirmed `culled on the card` and exposed a real bug - the widening counter was gated so it could only ever read zero. Corrected, widening accounts for 248 of 404 hides. Culling still does not pay for itself: CPU-bound scene, 192-block occluder, and Phase 6 is what changes that. 3,024 assertions pass. |
| 43 | 2026-08-23 | 0.3.68 packaged; 0.3.66 owner-run | - | Read the 0.3.66 run and was wrong twice. The view-epoch guard added last session refused ZERO comparisons while seven unsafe disagreements stood, because the epoch only advances on a global invalidation rather than on mouse movement; a frame-age bound replaces it. The 6.4% subdivision headroom was measured over a population that was 53% unjudgeable - the owner explained the setting (new terrain, nothing close) and the near ring of cached-but-masked sections around the camera accounted for it, so that figure was evidence for nothing and would have been reported as though it were. Fail-open now carries its cause. Then the owner asked whether the work was genuinely gated on playtesting, and it was not: the query cross-check has a noise floor above zero by construction, since the pyramid judges last frame's depth and the query reports a draw some frames earlier (G73). Correctness moved offline to a synthetic ridge with boxes poking out of it - which failed first run and taught the session's main finding: a box clears an occluder only by a whole texel of the level it is tested at, and bigger boxes are tested at coarser levels (G74). That gives a second, much cheaper candidate fix for the sky problem - sample more texels at a finer level, changing only the test rather than what is drawn. Offline it found about a quarter more hidden boxes; the shadow pass now measures it beside 4x4 subdivision over the same population so one run can choose. Also fixed a 160 ms one-time shader compile being reported as per-frame cost, and `.vhhzb why` landing on unjudgeable adjacent sections. 2,899 assertions pass; nothing new has run. |
| 42 | 2026-08-22 | 0.3.65 packaged; 0.3.58 human-played | - | Shipped Phase 3b - sections now record how tall their drawn geometry is, and the established renderer culls with that instead of a bedrock-to-sky box - then built Phase 4 to the point where it measures itself. Concluded from the height distribution that Phase 4 should be held; the owner vetoed it and was right, because that judged a screen-space question with a world-space statistic and the plan's "Phase 4 requires Phase 3b" claim was withdrawn with it. Measured why sections read as 146 blocks tall: one missing neighbour walls a section's edge to bedrock, so a single open side drops its floor 110 blocks (checked, not theorised). The depth pyramid costs 27 us of GPU time against 219 us of draw submission, and finds under half of distant terrain hidden within 1 km rising to ~100% past 2 km, over 16 million tests at two ground-level locations - and 0.0% from an aerial route, which is the honest answer for that view. Every not-hidden verdict was refused because the box overlaps sky, which makes cluster subdivision the thing that unlocks the rest rather than a later phase. Four defects reached hardware and every one was found by running: an unpaired shader Use(), a GLSL reserved word, an unfenced SSBO read costing 686 us/frame, and minimised frames counted as failures. Two owner playtests were wasted on diagnostics that could not be read (G72). Phase 4's correctness gate is NOT closed - 1,215 unsafe disagreements stand unexplained on the build the owner ran, and the fix has never been installed. 2,358 assertions pass. |
| 41 | 2026-08-22 | 0.3.57 packaged, never run | - | Built the drawing half of the GPU plan's Phase 3: opaque cached terrain can now be submitted as one multi-draw per page set instead of one call per section, behind `.vhindirect` and off by default. The shader was not ported but split - one body file included by two three-line wrappers differing only by a define - so "the fast path draws identical pixels" is a property of the arrangement rather than a review responsibility. Delayed occlusion is suspended under batching, deliberately, so the A/B needs it off on both sides. Reviewing the new code before anyone ran it found two defects: GL state was not restored after a refused batch (G65), and flipping the switch kept occlusion answers taken under the other path (G66). Added two instruments for symptoms nothing could measure: a frame timeline that separates "the frame hitched" from "the mod hitched", and a join stall line. Then, from an owner question about eliminating blocked terrain cheaply, found that every section is culled with a box spanning bedrock to sky because sections never recorded their vertical extent - harmless for the frustum test, close to fatal for a depth test, and enough to make Phase 4 measure a crippled version of itself. The plan had the idea as an optional field with no owner and no gate; it is now Phase 3b, ahead of Phase 4, and it is the only step that improves the established renderer regardless of the fast path. Phase 4 held at the owner's direction. 1,969 assertions and 1,466 doc checks pass; nothing has drawn a frame. |
| 40 | 2026-08-22 | 0.3.51 human-tested; 0.3.52 built | - | Matched cached-terrain lighting to the engine's own by decompiling the client and decoding its sunlight ramp offline. The dominant term was the light colour: vanilla builds its ambient from ReflectColor, floored at a blue night tint once the sun is down, while the mod used SunColor, which stays orange - the two hues invert after sundown. The plan's leading theory (light direction drifting) was disproven; a 22.7% daylight brightening nobody had noticed was found. Five corrections shipped behind .vhlight; the owner played 0.3.51 and reported it substantially better. Then narrowed MarkChanged to the edges a change actually touched: measured 2.00-2.03 mesh rebuilds per content change against 5.00, three runs, no frame-rate cost. That same instrumentation withdrew the section's headline claim - "90 MB of GPU upload every 15 seconds at a standstill" is a warm-up figure; settled steady state is 2.1 MiB, which also removes re-meshing as the leading micro-hitch suspect. 1,892 assertions and 1,453 doc checks pass. |
| 39 | 2026-08-22 | 0.3.50 | - | Built the regional opaque arenas and Phase 3's non-drawing half, then measured the phase's own gate: on the frozen BodanBoys route, 87-182 opaque submissions per frame collapse to 8-19 multi-draw batches - about 11x fewer, clearing the 90% requirement - with frame rates identical to the shadow-off run. Two corrections got there: pages must be allocated in vertex/index pairs or a section can never be drawn with its neighbours (G59), and the first measurement reported 6x-8x over a third of the world because it could not see its own misses (G60). Page size, not region shape, is the lever, and is now a setting. 1,841 assertions and 1,447 doc checks pass; nothing visible changed. |
| 38 | 2026-08-21 | 0.3.47 source; next artifact 0.3.48 | - | Completed primary-machine GPU feasibility and Phase 1's legacy-only renderer boundary: nonblocking GPU telemetry, validated compute/SSBO and exact private depth copy, shared exact GL-state restoration, generation-tagged lifecycle ownership and an optional CPU-only shadow that cannot draw or own GL resources. The Release build and 1,627 assertions pass; FPS and runtime-equivalence evidence remains owner-run. |
| 37 | 2026-08-21 | 0.3.40 source; next artifact 0.3.41 | - | Source-traced the current layered occlusion renderer and created a proposed GPU-driven follow-on plan: regional opaque arenas, indirect multi-draw, conservative HZB, separately gated cached-on-cached occlusion, packed quads and only-later GPU LOD/clusters. The GL 3.3 renderer remains complete; no implementation phase began and no game process was launched. |
| 36 | 2026-08-21 | 0.3.40 source; next artifact 0.3.41 | - | Replaced activity-driven row writes with per-pipeline 30-second RAM checkpoints and one worker SQLite transaction; moved sibling-cache blob reads off the game thread; made seasonal, GPU-mesh and CPU-section maintenance incremental; capped unavoidable GL query work; and aligned server manifests to 30 seconds. A warning-free Release build and 1,555 assertions pass, but no game process was launched. Established that every changed playable artifact advances the patch by exactly one. |
| 35 | 2026-08-20 | 0.3.40 | - | Corrected L1-L6 transitions to 512 through 16,384 blocks while retaining nearest-edge section distance; the owner reported enormous performance improvement with little visual loss. Added `.vhconfig` with six constrained handles on one logarithmic scale, a 512-step cached draw cap, full-number labels and Defaults/Cancel/Save. Legacy settings migrate without a wire, blob or schema change; 1,533 assertions pass. |
| 34 | 2026-08-20 | 0.3.37 | - | Delayed queries now measure real opaque terrain after vanilla depth and use available zero-sample answers to skip later submissions. The owner's 4,000-block hill view rose from about 170 FPS to nearly 500 stationary and roughly 250-350 in sampled motion. Local invalidation fixed the pause-only failure; mixed seams and a narrow turning-edge band fail open. Aggressive is default-on and accepted, with safe/extreme live profiles. |
| 33 | 2026-08-20 | 0.3.30 | - | A same-frame GPU query prototype reached 83% hidden boxes but changed 156→155 FPS and could not see the current vanilla hill because cached terrain ran first. Removed it, then moved the cached pass from opaque order 0.36 to 0.38 after vanilla's 0.37. Owner A/Bs gained 148→179 FPS in the valley and 590→651 looking down; minute distant changes were entirely acceptable. Post-vanilla depth rejection is default-on with `.vhocclusion off` retained. |
| 32 | 2026-08-20 | 0.3.27 | - | The renderer had frustum and ownership rejection but no own terrain occlusion, while opaque cached meshes were two-sided and unordered. Corrected all six solid face windings, enabled back-face culling, and submitted opaque sections nearest-first from reusable storage; water and thin surfaces remain two-sided. Owner A/Bs gained 218→260 FPS and 149→173 FPS with no visual change, so both defaults are accepted. A remaining ~150 versus >300 FPS direction swing across 4,000 blocks of mountainous cache makes conservative occlusion next; extreme-flight issues remain recorded but demoted. |
| 31 | 2026-08-20 | 0.3.23 | - | The water seams were never colour. The mesher walls off a section edge whose neighbour is missing, but decided "missing" from what is in RAM rather than from what the cache holds, and sections mesh nearest-first - so the outward side of nearly every one was walled off before its neighbour loaded, and nothing repaired it. Invisible on land, a permanent 3,200 block2 sheet of translucent water on an ocean. Human-confirmed fixed, shores and cliffs included. The playtest log then closed the join-fill-in regression at 6.6 s, and the owner's two-distance FPS comparison settled the mask default and produced G52: a fragment-side saving measured in a CPU-bound scene reports its cost and none of its benefit. Master fast-forwarded to 0.3.23. |
| 30 | 2026-08-19 | 0.3.22 | - | Four faults in one colour, all diagnosed offline from the owner's cache, the engine's shaders and one screenshot: a block's colour was a random pixel drawn once per section, the seasonal tint was one row of a sixteen-row map, grass-covered ground had red and blue exchanged and was missing the untinted dirt vanilla shows through it, and flat ground was shaded by sun angle where vanilla refuses to darken it. Albedo now reproduces vanilla's own blend exactly. |
| 29 | 2026-08-19 | 0.3.17 | - | Closed the band: the engine range-culls every terrain mesh per frame against the view distance, which no per-chunk signal reflects, so ownership held the trailing annulus forever. Human-confirmed fixed, and the per-cell mask became the default. |
| 28 | 2026-08-19 | 0.3.15 | - | Fixed a silent startup fault, made the per-cell mask actually work after an integer uniform the driver had rejected since 0.3.0, corrected G40 from the game IL, and disproved five explanations of the missing band, which remains open. |
| 27 | 2026-08-19 | 0.3.0-dev | — | Gave each vanilla chunk its own ground behind an opt-in gate, skipped fully replaced cached meshes for +7.3% frame rate, and fixed ownership latency found by playtesting at flight speed. |
| 26 | 2026-08-19 | 0.2.1 | — | Runtime-validated the readiness tracker, fixed a sweep that could lose ownership but never gain it, and gave the near handoff a measured radius; 1,225 assertions pass. |
| 25 | 2026-08-18 | 0.2.1 | — | Source-traced the installed 1.22.7 chunk-render lifecycle and added a bounded, pixel-neutral vanilla-readiness shadow tracker; 1,176 assertions pass. |
| 24 | 2026-08-18 | 0.2.1 | — | Source-fixed camera-relative color noise and near-transition sinking, added a conservative radial playtest handoff, and approved a bounded chunk-aware hybrid ownership plan. |
| 23 | 2026-08-18 | 0.2.1 | — | Attributed the remaining server-assist tail to synchronous hot-path progress logging, removed it, added GC/phase diagnostics, and passed 1,058 checks plus a guarded 273-section fix run. |
| 22 | 2026-08-18 | 0.2.1 | — | Proved integrated sibling-cache exact-key retry/adoption and hard mip interruption/recovery through a fresh zero-obligation postcheck; 1,056 checks pass. |
| 21 | 2026-08-18 | 0.2.1 | — | Proved renderer budgets through 3,132 cached sections; added revision-acknowledged saves, retry/coalescing/shutdown draining, and 1,050 passing checks. |
| 20 | 2026-08-18 | 0.2.1 | — | Time/byte-budgeted mesh snapshots and GPU uploads; safe mesh replacement and queue/GL telemetry; 1,002 checks pass. |
| 19 | 2026-08-18 | 0.2.1 | — | Replaced per-frame whole-dirty-set scans with an incremental coarse-cell priority index; 995 checks and a 601-section functional route converged cleanly. |
| 18 | 2026-08-18 | 0.2.1 | — | Rejected invisible quadtree subtrees without visibility-driven eviction; a controlled 601-section pair cut selected nodes 64.2% and average traversal 19.8%. |
| 17 | 2026-08-18 | 0.2.1 | — | Interrupted after a durable mip flag, recovered one obligation to clean convergence, and proved a later fresh process loaded zero obligations. |
| 16 | 2026-08-18 | 0.2.1 | — | Added semantic mip/persistence convergence proof; a 2,401-column movement run and 601-section fresh restart both drained cleanly. |
| 15 | 2026-08-18 | 0.2.1 | — | Moved server-assist blobs to an ordered read-only worker; 395 sections transferred and a 17.481 ms read no longer blocked assist service. |
| 14 | 2026-08-18 | 0.2.1 | — | Added semantic generation/assist scenarios; fixed an early-join request drop; 395 live sections transferred and synchronous server reads reached 68.755 ms. |
| 13 | 2026-08-18 | 0.2.1 | — | Added cache-state and server-completion benchmark proofs; warm join and a 3,249-position sweep completed without ≥25 ms VH/server ticks. |
| 12 | 2026-08-17 | 0.2.1 | — | Added server pipeline/sweep/generation/assist telemetry and real stats A/B controls; warmed stationary pairs measured about 0.7% average-FPS overhead. |
| 11 | 2026-08-17 | 0.2.1 | — | Added a one-way clean-cache capture-frontier route and endpoint cooldown; two runs had no ≥25 ms VH ticks and bounded, convergent backlog. |
| 10 | 2026-08-17 | 0.2.1 | — | Reclassified the long route as warm-cache traversal; recorded a positive human smoothness/clipping review; made uncached-terrain validation explicit. |
| 9 | 2026-08-17 | 0.2.1 | — | Ran the full corrected movement/rotation route; bounded capture publication by time/bytes; added result backpressure, epoch rejection, and before/after evidence. |
| 8 | 2026-08-17 | 0.2.1 | — | Added deterministic moving/rotating benchmark routes; corrected PI-centred camera pitch; narrowed old sky-biased evidence and preserved four mip CSVs. |
| 7 | 2026-08-17 | 0.2.1 | — | Moved foreign inflation/structural decode to the storage owner; retained request state until publication; added opt-in per-phase allocation telemetry. |
| 6 | 2026-08-17 | 0.2.1 | — | Smoothed sweep/generation/assist allowances across ticks; time/byte-bounded owning-thread installs with FIFO progress and queue telemetry. |
| 5 | 2026-08-17 | 0.2.1 | — | Cached opaque/water mesh bounds; made steady far-distance calculation O(1); stabilized projection with quantized growth and delayed shrink. |
| 4 | 2026-08-17 | 0.2.1 | — | Moved sibling-cache key discovery to a dedicated delta publisher; applied manifests once; made local/server failures explicitly retryable or terminal. |
| 3 | 2026-08-17 | 0.2.1 | — | Instrumented and reproduced game-tick spikes; moved versioned mip construction off-thread; two before/after routes eliminated measured ≥25 ms tick hitches. |
| 2 | 2026-08-17 | 0.2.1 | — | Supplied snapshot matched to fork history; working branch rebased onto release 0.2.1; documentation and performance plan reconciled with existing fork fixes. |
| 1 | 2026-08-17 | 0.2.0 | — | Source-traced main-thread performance review; GitHub fork association; lifetime-tiered documentation workflow established; remediation plan approved and preserved. |
